apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: proshop
  labels:
    app: backend

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
